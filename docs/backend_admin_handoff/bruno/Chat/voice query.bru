meta {
  name: voice query
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/chat/voice
  body: multipartForm
  auth: inherit
}

params:query {
  conversation_id: 1
}

headers {
  Authorization: Bearer {{jwt}}
  
}

body:multipartForm {
  audio: @file(./audio.wav)
}
